# Documentation index — {{PROJECT_NAME}}

> Entry point for all documentation. Each entry says what it is and when to read it.
> Rule: every new document gets one line here. No loose docs.
> (For large projects, organize into subfolders: `design/`, `research/`, `specs/` — still one line per doc.)

**Status {{DATE}}:** project initialized with hi-claude.

## Documents

*(no documents yet — the first generated doc gets indexed here)*
